-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;

CREATE TABLE IF NOT EXISTS PUBLIC.JOGOS (
        FIXTURE_ID CHARACTER VARYING(50) COLLATE PG_CATALOG."default" NOT NULL,
        PARTICIPANT1_ID INTEGER,
        PARTICIPANT1_NAME CHARACTER VARYING(255) COLLATE PG_CATALOG."default",
        PARTICIPANT1_SHORT_NAME CHARACTER VARYING(100) COLLATE PG_CATALOG."default",
        PARTICIPANT1_ABBR CHARACTER VARYING(50) COLLATE PG_CATALOG."default",
        PARTICIPANT2_ID INTEGER,
        PARTICIPANT2_NAME CHARACTER VARYING(255) COLLATE PG_CATALOG."default",
        PARTICIPANT2_SHORT_NAME CHARACTER VARYING(100) COLLATE PG_CATALOG."default",
        PARTICIPANT2_ABBR CHARACTER VARYING(50) COLLATE PG_CATALOG."default",
        SPORT_ID INTEGER,
        SPORT_NAME CHARACTER VARYING(100) COLLATE PG_CATALOG."default",
        TOURNAMENT_ID INTEGER,
        TOURNAMENT_NAME CHARACTER VARYING(255) COLLATE PG_CATALOG."default",
        TOURNAMENT_SLUG CHARACTER VARYING(255) COLLATE PG_CATALOG."default",
        CATEGORY_NAME CHARACTER VARYING(255) COLLATE PG_CATALOG."default",
        CATEGORY_SLUG CHARACTER VARYING(255) COLLATE PG_CATALOG."default",
        SEASON_ID CHARACTER VARYING(50) COLLATE PG_CATALOG."default",
        STATUS_ID INTEGER,
        STATUS_NAME CHARACTER VARYING(100) COLLATE PG_CATALOG."default",
        HAS_ODDS BOOLEAN,
        START_TIME TIMESTAMP WITH TIME ZONE,
        TRUE_START_TIME TIMESTAMP WITH TIME ZONE,
        TRUE_END_TIME TIMESTAMP WITH TIME ZONE,
        API_UPDATED_AT TIMESTAMP WITH TIME ZONE,
        EXTERNAL_PROVIDERS JSONB,
        CRIADO_EM TIMESTAMP WITHOUT TIME ZONE DEFAULT CURRENT_TIMESTAMP,
        ATUALIZADO_EM TIMESTAMP WITHOUT TIME ZONE DEFAULT CURRENT_TIMESTAMP,
        CONSTRAINT JOGOS_PKEY PRIMARY KEY (FIXTURE_ID)
);

CREATE TABLE IF NOT EXISTS PUBLIC.JOGOS_ODDS (
        ID SERIAL NOT NULL,
        FIXTURE_ID CHARACTER VARYING(50) COLLATE PG_CATALOG."default" NOT NULL,
        ODDS_BRUTAS JSONB NOT NULL,
        CRIADO_EM TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
        ATUALIZADO_EM TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
        CONSTRAINT JOGOS_ODDS_PKEY PRIMARY KEY (ID),
        CONSTRAINT UNIQUE_FIXTURE_ID UNIQUE (FIXTURE_ID)
);

ALTER TABLE IF EXISTS PUBLIC.JOGOS_ODDS
ADD CONSTRAINT FK_JOGO FOREIGN KEY (FIXTURE_ID) REFERENCES PUBLIC.JOGOS (FIXTURE_ID) MATCH SIMPLE ON UPDATE NO ACTION ON DELETE CASCADE;

CREATE INDEX IF NOT EXISTS IDX_ODDS_FIXTURE ON PUBLIC.JOGOS_ODDS (FIXTURE_ID);

END;



